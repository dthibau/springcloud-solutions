spring:
  application:
    name: account-service
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
 

  jpa:
    show-sql: true
    
logging:
  level:
    org.springframework.web.reactive: DEBUG
  
