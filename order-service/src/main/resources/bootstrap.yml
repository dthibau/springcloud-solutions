spring:
  application:
    name: order-service
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true

    loadbalancer:
      ribbon:
        enabled: false
        

logging:
  level:
    org.springframework.security: debug 
